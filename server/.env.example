# Server port
PORT=5000

# Where the frontend runs (used for CORS and socket origin checks)
CLIENT_URL=http://localhost:3000

# MongoDB connection string (local or Atlas)
MONGODB_URI=your_mongodb_atlas_connection_string
